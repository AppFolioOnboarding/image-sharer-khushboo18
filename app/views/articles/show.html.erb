<div class="container">
<h4>Your link has been saved!</h4>
<p>
  Title: <%= @article.text %>
</p>
<%= image_tag(@article.link, :class => 'image') %>
<% if !@article.tag_list.empty? %>
  <p>Tags: 
  <% @article.tag_list.each do |tag| %>
    <%= link_to tag, articles_path(tag: tag) %>
  <% end %>
  </p>
<% end %>
<p>
<%= link_to 'Delete Image', @article,
              method: :delete,
              data: { confirm: 'Are you sure you want to delete this image?' } %></td>
</p>
</div>
